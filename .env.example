# Example environment variables — DO NOT COMMIT real secrets
# Copy this file to `.env` and fill the values locally.

# Discord (if you use a bot or webhook)
DISCORD_TOKEN=
DISCORD_WEBHOOK_URL=

# Third-party API keys
API_KEY=
API_SECRET=

# Database
DATABASE_URL=

# Notes:
# - Never commit the real `.env` file. The repository's .gitignore already ignores `.env` files.
# - If you accidentally committed secrets, rotate them immediately and remove them from Git history.
